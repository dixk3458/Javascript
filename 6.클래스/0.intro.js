/**
 * 자바스크립트의 내부 구현사항은 프로토타입을 이용해서
 * 생성자 함수를 지원한다.
 * 따라서 자바스크립트에서 객체를 만들때 생성자 함수를 이용한다.
 * 
 * 클래스는 객체를 손쉽게 만들수있는 템플릿이다(청사진,틀).
 * 개발자들은 서로 밀접하게 연관있는 데이터를 하나의 객체로 만들어 나가는데,
 * 이러한방식을 객체지향 프로그래밍이라고 말한다.
 * 
 * 요즘은 클래스를 이용해서 조금 더 손쉽게 객체를 만들어낸다.
 * 
 * 인스턴스 = 클래스를 통해 만들어진 객체
 * 
 */